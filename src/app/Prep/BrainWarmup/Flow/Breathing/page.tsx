// Minimal placeholder page that renders nothing and causes no imports or side-effects.
export default function BreathingPage() {
  return null; // Returning null is valid in React/Next.js and compiles cleanly
}
